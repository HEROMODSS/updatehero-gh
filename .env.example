# These are set in Vercel → Project → Settings → Environment Variables.
# This file is just a reference — Vercel serverless functions read them,
# nothing here is used by the browser directly (no VITE_ prefix needed).

ADMIN_PASSWORD=choose-a-strong-password
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_OWNER=HEROMODSS
